# Models Folder

Runtime models are trained in memory and cached by Streamlit for interactivity.

Use this folder for optional persisted `joblib` artifacts if you decide to productionize scheduled retraining.
